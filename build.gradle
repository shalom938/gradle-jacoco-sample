//plugins {
//    id 'org.javamodularity.moduleplugin' version '1.8.10' apply false
//}


//TODO:
// toolchain
// add another provider module that prints all modules loaded
// change the package name org.gradle to sk
// try with resources example
// use var
// add logging

allprojects {
    version = '1.0.0'
    group = 'org.sk.sample.modules'

    repositories {
        mavenCentral()
    }
}

subprojects {

    tasks.withType(Test).configureEach {
        useJUnitPlatform()
    }
}
