plugins {
    id 'application'
    alias(pluginslibs.plugins.moduleplugin)
}


//TODO:
// integration tests
// create a container module, one that only requires all the providers,plus has gradle dependencies on them,
// then from here only add one dependency. and in module-info require the providers container.
// if its a java module maybe it will produce an empty jar.
// add exports .. to in module-info where necessary to really hide packages
// change StringJournal to its own module and use reflection, keep the constructor provate and open
// reflection to the message-journal module

dependencies {
    implementation project(':messaging')
    implementation project(':utilities')
    implementation libs.commons.text

    testImplementation testinglibs.junit.jupiter
}

application {
    mainClass = 'org.sk.sample.app.Main'
}


test {
    onlyIf {
        project.hasProperty("aaaaaaaaaa")
    }
    testLogging {
        showStandardStreams project.hasProperty("show.unit.tests.stdout")
        events "passed", "skipped", "failed"
        showStackTraces true
        exceptionFormat 'full'
        showCauses true
    }
}